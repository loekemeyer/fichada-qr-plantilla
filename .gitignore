# config.js SÍ se sube: solo lleva la URL y el anon key, que son públicos.
# Los secretos (firma HMAC, clave de pantalla, clave de panel) viven en Postgres.

# Basura del sistema / editores
.DS_Store
Thumbs.db
*.log
.env
.env.*
node_modules/
.vscode/
.idea/
